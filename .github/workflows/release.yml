name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@e16e3f81586da64dd5c690da9ab4b38592294163 # v8.8.0
    permissions:
      contents: write  # to create the GitHub Release
